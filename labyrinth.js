var cols = 26, // number columns 
        rows = 19, // number rows 
        size = 30, // tamanho de cada tiles
        tiles = []; // tiles matris
function getAvailable(i, j) {
    var available = [],
            len = tiles[i][j].neighbours.length;
    for (var ix = 0; ix < len; ix++) {
        var t = tiles[i][j]
    }
}
